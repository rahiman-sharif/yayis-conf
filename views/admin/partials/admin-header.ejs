<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin - <%= title %></title>
    <link rel="stylesheet" href="/css/admin-style.css">
</head>
<body>
    <div class="admin-container">
        <aside class="admin-sidebar">
            <div class="admin-logo">
                <a href="/admin"><img src="/images/brand/logo.png" alt="Yayi's Confectionery Admin Logo" id="admin-logo-image"></a>
                <h2>Yayi's Admin</h2>
            </div>
            <nav>
                <ul>
                    <li><a href="/admin" class="<%= title === 'Admin Panel' ? 'active' : '' %>">Dashboard</a></li>
                    <li><a href="/admin/menu" class="<%= title === 'Menu Manager' || title === 'Add New Cake' || title === 'Edit Cake' ? 'active' : '' %>">Menu Manager</a></li>
                    <li><a href="/admin/projects" class="<%= title === 'Project Manager' || title === 'Add New Project' || title === 'Edit Project' ? 'active' : '' %>">Project Manager</a></li>
                    <li><a href="/admin/stalls" class="<%= title === 'Stall Events Manager' || title === 'Add New Stall Event' || title === 'Edit Stall Event' ? 'active' : '' %>">Stall Events</a></li>
                    <li><a href="/admin/config" class="<%= title === 'WhatsApp Configuration' ? 'active' : '' %>">Business Whatsup Settings</a></li>
                    <li><a href="/" target="_blank">View Site</a></li>
                </ul>
            </nav>
        </aside>
        <main class="admin-main-content">
            <header class="admin-header">
                <h1><%= title %></h1>
            </header>
